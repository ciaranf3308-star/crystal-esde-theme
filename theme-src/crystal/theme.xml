<?xml version="1.0" encoding="UTF-8"?>
<theme>
    <!-- Crystal v2 root theme: palette variables (dark base + light
         <variant>) + shared view layout. -->
    <include>./variables.xml</include>
    <include>./views.xml</include>
</theme>
