<?xml version="1.0" encoding="UTF-8"?>
<theme>
    <!-- Crystal v2 per-system overrides for nds: explicit asset paths.
         Element names match views.xml so these merge cleanly over the shared
         layout; the includes keep this file standalone-safe. -->
    <include>./../variables.xml</include>
    <include>./../views.xml</include>
    <view name="system">
        <image name="sysBackground">
            <path>./../${crystalBgDir}/nds.webp</path>
        </image>
        <image name="sysLogo">
            <path>./../${crystalLogoDir}/nds.png</path>
        </image>
    </view>
    <view name="gamelist">
        <image name="gameBackground">
            <path>./../${crystalBgDir}/nds.webp</path>
        </image>
        <image name="gameArt">
            <default>./../${crystalLogoDir}/nds.png</default>
        </image>
    </view>
</theme>
